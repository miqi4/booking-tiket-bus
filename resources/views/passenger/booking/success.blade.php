<x-layouts.passenger title="E-Tiket Berhasil - PO. Akas">
<main class="max-w-3xl mx-auto px-gutter py-lg w-full"><div class="bg-surface-container-lowest rounded-xl border border-outline-variant p-lg text-center"><div class="w-16 h-16 bg-secondary-container text-on-secondary-container rounded-full mx-auto flex items-center justify-center mb-md"><span class="material-symbols-outlined text-[40px]">check</span></div><h1 class="font-h1 text-h1 text-primary mb-sm">Pembayaran Berhasil</h1><p class="text-on-surface-variant mb-md">Kode booking {{ $booking->booking_code }} sudah confirmed.</p><div class="text-left border border-outline-variant rounded-xl p-md mb-md"><p class="font-h3 text-h3">{{ $booking->schedule->busRoute->originCity->name }} - {{ $booking->schedule->busRoute->destinationCity->name }}</p><p class="text-on-surface-variant">{{ $booking->schedule->departure_at->translatedFormat('d F Y H:i') }} WIB, {{ $booking->schedule->bus->name }}</p><div class="mt-sm space-y-xs">@foreach($booking->passengers as $passenger)<div class="flex justify-between"><span>{{ $passenger->name }}</span><span class="font-label-form text-primary">Kursi {{ $passenger->seat->seat_number }}</span></div>@endforeach</div></div><div class="flex flex-col md:flex-row gap-sm justify-center"><a class="px-md py-sm bg-primary text-on-primary rounded-lg" href="{{ route('dashboard.bookings') }}">Lihat Riwayat</a><a class="px-md py-sm border border-primary text-primary rounded-lg" href="{{ route('home') }}">Pesan Lagi</a></div></div></main>
</x-layouts.passenger>
